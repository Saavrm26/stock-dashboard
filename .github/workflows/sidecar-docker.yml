name: Build and push sidecar image

on:
  push:
    branches: [main]
    paths:
      - "stock-dashboard-api-sidecar/**"
      - "common/**"
      - ".github/workflows/sidecar-docker.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: stock-dashboard-api-sidecar
          file: stock-dashboard-api-sidecar/Dockerfile
          build-contexts: common=common
          platforms: linux/amd64
          push: true
          tags: ghcr.io/saavrm26/stock-dashboard/sidecar:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
