apiVersion: v1
kind: Service
metadata:
  name: stock-dashboard-frontend-service
  labels:
    app: stock-dashboard-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: stock-dashboard-frontend
