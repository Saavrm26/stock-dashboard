apiVersion: apps/v1
kind: Deployment
metadata:
  name: stock-dashboard-api-deployment
  labels:
    app: stock-dashboard-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stock-dashboard-api
  template:
    metadata:
      labels:
        app: stock-dashboard-api
    spec:
      containers:
      - name: app
        env:
          - name: DB_URL
            valueFrom:
              configMapKeyRef:
                key: db_url
                name: infra-config
          - name: DB_USER
            valueFrom:
              configMapKeyRef:
                key: db_user
                name: infra-config
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "1024Mi"
            cpu: "500m"
          limits:
            memory: "2048Mi"
            cpu: "1000m"
