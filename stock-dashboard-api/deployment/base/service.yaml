apiVersion: v1
kind: Service
metadata:
  name: stock-dashboard-api-service
  labels:
    app: stock-dashboard-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: stock-dashboard-api
