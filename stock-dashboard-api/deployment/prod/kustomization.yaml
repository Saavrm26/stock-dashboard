apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      env: prd

resources:
  - ../base
#  - ingress.yaml

patches:
  - path: patch-deployment.yaml

configMapGenerator:
  - name: aurora-cacrts
    files:
      - ./global-bundle.pem