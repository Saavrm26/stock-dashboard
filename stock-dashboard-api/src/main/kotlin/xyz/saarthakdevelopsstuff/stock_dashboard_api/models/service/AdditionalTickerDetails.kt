package xyz.saarthakdevelopsstuff.stock_dashboard_api.models.service

// Placeholder for the remaining TickerDetails proto fields (financials, officer
// lists, exchange metadata, etc). Intentionally left empty for now - only the
// core fields on TickerDetails are modeled. Populate this class in a follow-up
// once a consumer actually needs one of the additional ~130 proto fields.
class AdditionalTickerDetails
