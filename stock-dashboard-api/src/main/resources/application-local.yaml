spring:
  datasource:
    url: jdbc:postgresql://${DB_URL}
    username: ${DB_USER}
    password: postgres

#  config:
#    import:
#      - "aws-secretsmanager:test/password?prefix=db."


  jpa:
    show-sql: true
