spring:
  datasource:
    # This doesn't actually exist
    url: jdbc:postgresql://${DB_URL}
    username: ${DB_USER}

#  config:
#    import:
#      - "aws-secretsmanager:test/password?prefix=db."


  jpa:
    show-sql: false
